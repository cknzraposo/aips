# Government-source documents

This directory is reserved for NZ government documents that are explicitly confirmed as redistributable (e.g., NZGOAL / CC BY).

Current status: no government PDFs from the legacy source store were committed yet because per-document licence confirmation is still pending.

Until confirmed, these sources remain classified as `fetch-on-demand` or `manual` in `data/MANIFEST.csv`.
